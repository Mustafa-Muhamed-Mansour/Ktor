package com.example.plugins

import io.ktor.server.application.*
import io.ktor.server.response.*
import io.ktor.server.routing.*

/* fun Application.configureWithoutInstallRouting() {
    routing {
        get("/") {
            call.respond("Hello World, without install")
        }
    }
} */

/* fun Routing.configureWithInstallRouting() {
        get("/") {
            call.respond("Hello World, with install")
        }
} */